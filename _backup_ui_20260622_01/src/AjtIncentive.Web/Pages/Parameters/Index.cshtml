@page
@{
    ViewData["Title"] = "Parameters";
}

<div class="content-panel">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-2 mb-3">
        <div>
            <div class="eyebrow">Config</div>
            <h1 class="h3 mb-1">Parameters</h1>
            <p class="text-muted mb-0">หน้า placeholder สำหรับ threshold, rate, weight และ master data</p>
        </div>
    </div>

    <div class="alert alert-info mb-0">
        หน้านี้พร้อมต่อยอดเป็นหน้าจัดการค่าพารามิเตอร์ทางธุรกิจ
    </div>
</div>